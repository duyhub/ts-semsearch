# Embedding provider comparison

Dense-retrieval quality per provider on the **tune** split (generated by `scripts/embedding_choice.py`).

| Provider | Model | Status | Recall@5 | NDCG@5 |
|---|---|---|---|---|
| local | `BAAI/bge-m3` | ok | 0.929 | 0.881 |
| bedrock-cohere | `cohere.embed-multilingual-v3` | skipped (provider 'bedrock-cohere' not wired yet (local is the default; Bedrock is a later, credential-gated path). See FR-10.) | — | — |
| bedrock-titan | `amazon.titan-embed-text-v2:0` | skipped (provider 'bedrock-titan' not wired yet (local is the default; Bedrock is a later, credential-gated path). See FR-10.) | — | — |

**Chosen: `local` (bge-m3)** — primary provider per D1 (local-first). Best measured NDCG@5: `local` (0.881).

Bedrock providers are compared only when AWS credentials are available; the demo path never depends on them (NFR-3).
